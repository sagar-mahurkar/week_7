name: Main

on:
  push:
    branches:
      - main
    paths:
      - '**.py'
      - '**.ipynb'
      - 'requirements.txt'
      - 'artifacts/**'
  pull_request:
    branches:
      - main
  workflow_dispatch:  # manual trigger

permissions:
  contents: write
  pull-requests: write

jobs:
  test_model:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        data_version: [v1.0, v2.0]   # your DVC/Git tags or branches

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - uses: iterative/setup-cml@v2

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # Configure DVC to use your GCS bucket
      - name: Setup DVC Remote
        env:
          GCP_BUCKET: "gs://mlops-lively-nimbus-473407-m9"
        run: |
          dvc remote add -f -d gcs_remote $GCP_BUCKET

      # Authenticate to GCP using service account key from GitHub secret
      - name: Authenticate to GCP
        env:
          GCP_CREDENTIALS_JSON: ${{ secrets.GCP_CREDENTIALS_JSON }}
        run: |
          echo "$GCP_CREDENTIALS_JSON" > /tmp/gcp_key.json
          gcloud auth activate-service-account --key-file=/tmp/gcp_key.json
          dvc remote modify gcs_remote credentialpath /tmp/gcp_key.json

      # Optional: verify that authentication works
      - name: Verify GCP access
        run: |
          gsutil ls gs://mlops-lively-nimbus-473407-m9 || echo "⚠️ Could not list bucket contents"

      # Checkout the correct version of data and pull from DVC remote
      - name: Checkout and Pull DVC Data
        run: |
          echo "Checking out data version: ${{ matrix.data_version }}"
          git fetch --tags
          git checkout ${{ matrix.data_version }}
          echo "===== DVC CONFIG ====="
          dvc remote list
          dvc remote list --local || true
          cat .dvc/config || true
          echo "===== PULLING DATA ====="
          dvc pull -v || exit 1
          echo "✅ Pulled DVC data successfully"
          echo "===== FILES IN WORKSPACE ====="
          ls -R .

      # Verify that data.csv exists before training
      - name: Verify data.csv exists
        run: |
          if [ ! -f data.csv ] && [ ! -f data/data.csv ]; then
            echo "❌ data.csv not found after DVC pull!"
            ls -R .
            exit 1
          fi
          echo "✅ data.csv found!"

      # Run your training script
      - name: Train the model
        run: |
          python train.py > main_output.txt 2>&1 || true

      # Display training log for debugging
      - name: Debug — show training output
        run: |
          echo "=== Training Log ==="
          cat main_output.txt || true

      # Post a comment on the PR with results
      - name: Create CML report
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "## Accuracy Output for ${{ matrix.data_version }}" > report.md
          echo "\`\`\`" >> report.md
          cat main_output.txt >> report.md
          echo "\`\`\`" >> report.md
          cml comment create --publish report.md